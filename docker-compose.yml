services:
  hello-agent:
    image: hello-agent:1.0.0  # 构建后镜像的名称和标签   
    # build:
    #   context: .
    #   dockerfile: dockerfiles/Dockerfile
    environment:
      # 设定应用名称
      APP: hello-agent
      # 阿里百炼平台 api key
      DASHSCOPE_API_KEY: ${DASHSCOPE_API_KEY}
      TAVILY_API_KEY: ${TAVILY_API_KEY}
      OLLAMA_BASE_URL: http://host.docker.internal:11434
    extra_hosts:
      # 容器能通过 host.docker.internal 访问宿主机的一种标准做法
      - "host.docker.internal:host-gateway"
    networks:
      - default  
    volumes:
      - "~/.ssh:/root/.ssh:ro" # 挂载宿主机的ssh配置文件，方便推拉代码（只读）
      - ".:/root/hello-agent"
    working_dir: /root/hello-agent
    command: ["sleep", 'infinity']

  
  # ollama-server:
  #   image: ollama/ollama:latest
  #   pull_policy: always
  #   restart: unless-stopped
  #   entrypoint: ["/usr/bin/bash", "/entrypoint.sh"]
  #   environment:
  #     - OLLAMA_MODELS=${OLLAMA_MODELS:-qwen2.5:3b}
  #     # '/Users/henryzhu/data/qwen2.5:0.5b'
  #   networks:
  #     - default
  #   volumes:
  #     - ./scripts/entrypoint.sh:/entrypoint.sh # 用于修改 entrypoint.sh 文件
  #     # - ollama_volume:/root/.ollama # 貌似不需要挂载   
  #   ports:
  #     - "11434:11434"
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]
  #   healthcheck:
  #     test: ["CMD-SHELL", "curl --silent --fail http://localhost:11434/ || exit 1"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 5
  #     start_period: 30s
  #   logging:
  #     driver: "json-file"
  #     options:
  #       max-size: "10m"
  #       max-file: "3"

  # open-webui:
  #   image: ghcr.io/open-webui/open-webui:main
  #   ports:
  #     - "3000:8080" # http://localhost:3000
  #   environment:
  #     - OLLAMA_BASE_URL=http://ollama-server:11434
  #     - WEBUI_NAME="Your Local AI Assistant" # Sets the main WebUI name. Appends (Open WebUI) if overridden.
  #     - WEBUI_AUTH=False
  #     - WEBUI_SECRET_KEY=t0p-s3cr3t
  #   restart: unless-stopped


# -- 网络名称，用于容器间通信
networks:
  default:
    name: helloagent-network
